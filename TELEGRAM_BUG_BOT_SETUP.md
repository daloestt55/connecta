# Настройка Telegram Bot для Bug Reports

## Шаг 1: Создайте Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя для бота (например, `Connecta Bug Reports`)
4. Введите username для бота (например, `connecta_bugs_bot`)
5. **Сохраните токен**, который выдаст BotFather (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Получите свой Chat ID

### Вариант 1: Через @userinfobot
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение боту
3. Бот пришлёт ваш `Id` — это и есть ваш Chat ID (например, `123456789`)

### Вариант 2: Через @getmyid_bot
1. Найдите [@getmyid_bot](https://t.me/getmyid_bot)
2. Нажмите Start
3. Бот пришлёт `Your user ID` — это ваш Chat ID

### Вариант 3: Для группового чата (если хотите получать в группу)
1. Добавьте вашего бота в группу
2. Откройте [@getidsbot](https://t.me/getidsbot)
3. Перешлите любое сообщение из группы в @getidsbot
4. Бот пришлёт Chat ID группы (будет отрицательным, например `-987654321`)

## Шаг 3: Настройте переменные окружения

Откройте файл `.env` и заполните:

```env
TELEGRAM_BUG_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BUG_CHAT_ID=123456789
```

**Замените значения на свои!**

## Шаг 4: Перезапустите mock-server

1. Остановите текущий mock-server (Ctrl+C в терминале)
2. Перезапустите:
   ```bash
   node mock-server.js
   ```

В консоли вы должны увидеть:
```
🚀 Mock Supabase Edge Functions running on http://localhost:54321
🐛 Bug Report endpoint: http://localhost:54321/functions/v1/bug-report
```

## Шаг 5: Протестируйте

1. Откройте приложение (localhost:5174)
2. Зайдите в Settings (⚙️ в сайдбаре или `Ctrl+Shift+S`)
3. Выберите вкладку **Report Bug**
4. Заполните форму и отправьте
5. Проверьте Telegram — сообщение должно прийти!

---

## Формат сообщения, которое приходит в Telegram:

```
🐛 Connecta Bug Report

📋 Category: bug
📝 Title: Пример бага
💬 Description: Подробное описание проблемы

👤 User: username
📧 Email: user@example.com
🆔 User ID: user-id-123
🔗 Page: http://localhost:5174/
🖥️ Agent: Mozilla/5.0...
⏰ Time: 2026-02-24T12:34:56.789Z
```

---

## Для production (Supabase Edge Functions)

Когда будете деплоить на Supabase:

1. Откройте Supabase Dashboard
2. Перейдите в Project Settings → Edge Functions → Environment Variables
3. Добавьте:
   - `TELEGRAM_BUG_BOT_TOKEN` = ваш токен
   - `TELEGRAM_BUG_CHAT_ID` = ваш chat ID
4. Deploy Edge Function:
   ```bash
   supabase functions deploy bug-report
   ```

---

## Troubleshooting

### Сообщения не приходят

1. **Проверьте токен и chat ID** в `.env`
2. **Убедитесь, что mock-server перезапущен** после изменения `.env`
3. **Проверьте консоль mock-server** — там будут ошибки, если что-то не так
4. **Запустите бота** — отправьте `/start` вашему боту в Telegram

### "Failed to send to Telegram"

- Проверьте правильность токена
- Убедитесь, что бот не заблокирован/удалён
- Проверьте, что chat ID корректный (только цифры, возможно с минусом для групп)

### Бот не отвечает

- Убедитесь, что вы отправили `/start` боту
- Попробуйте создать нового бота через @BotFather

---

## Безопасность

⚠️ **НЕ КОММИТЬТЕ .env В GIT!**

.env уже добавлен в .gitignore, но всё равно будьте осторожны:
- Токен бота — это ключ доступа к вашему боту
- Никому не показывайте токен
- Если токен утёк — создайте нового бота через @BotFather

---

Готово! Теперь все баг-репорты будут приходить вам в Telegram 🎉
