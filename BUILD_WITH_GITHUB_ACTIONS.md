# 🚀 Сборка через GitHub Actions

## Преимущества
✅ Нет проблем с правами доступа  
✅ Автоматическая сборка при push  
✅ Создание релизов автоматически  
✅ Сборка на чистом окружении  
✅ Хранение всех версий в GitHub  

---

## 📝 Шаг 1: Подготовка

### Проверьте файлы
Убедитесь что у вас есть:
- ✅ `.github/workflows/electron-build.yml` - конфигурация сборки
- ✅ `electron/main.ts` и `electron/preload.ts` - файлы Electron
- ✅ `electron-builder.json` - конфигурация установщика
- ✅ `package.json` с скриптом `build:win`

---

## 🔧 Шаг 2: Коммит и Push

```powershell
# 1. Добавьте все файлы
git add .

# 2. Создайте коммит
git commit -m "Add Electron desktop build with GitHub Actions"

# 3. Отправьте на GitHub
git push origin main
```

---

## 🎯 Шаг 3: Запуск сборки

### Вариант A: Автоматический (при push)
Сборка запустится автоматически после `git push`

### Вариант B: Ручной запуск
1. Откройте репозиторий на GitHub.com
2. Перейдите в **Actions** (вкладка сверху)
3. Выберите **Build Electron Desktop**
4. Нажмите **Run workflow** → **Run workflow**

---

## 📦 Шаг 4: Скачивание результата

### Способ 1: Artifacts (для любого коммита)
1. GitHub → **Actions**
2. Выберите последний успешный запуск (зеленая галочка ✅)
3. Прокрутите вниз до раздела **Artifacts**
4. Скачайте архив **connecta-windows-[hash].zip**
5. Распакуйте и найдите `.exe` файлы

### Способ 2: Release (для версий с тегами)
Если хотите создать официальный релиз:

```powershell
# 1. Создайте тег версии
git tag v1.0.0

# 2. Отправьте тег на GitHub
git push origin v1.0.0
```

Затем:
1. GitHub → **Releases** (справа на главной странице)
2. Найдите созданный релиз **v1.0.0**
3. Скачайте установщик напрямую

---

## 🔍 Мониторинг прогресса

### В терминале
```powershell
# Откройте браузер с Actions
start https://github.com/daloestt55/connecta/actions
```

### Статусы
- 🟡 **Желтый кружок** - Сборка в процессе (5-10 минут)
- ✅ **Зеленая галочка** - Успешно! Можно скачивать
- ❌ **Красный крест** - Ошибка (смотрите логи)

### Где смотреть логи
1. GitHub → Actions
2. Кликните на запуск
3. Кликните на **Build Windows Desktop**
4. Читайте вывод каждого шага

---

## 🎉 Результат

После успешной сборки вы получите:

### В Artifacts (всегда):
- `Connecta-Setup-1.0.0.exe` - Полный установщик (~80-100 MB)
- `Connecta-Portable-1.0.0.exe` - Портативная версия (~120-150 MB)
- `latest.yml` - Файл для auto-update

### В Releases (если создали тег):
То же самое + автоматическое описание релиза

---

## 🔥 Quick Start (копируй-вставляй)

```powershell
# Добавить файлы, закоммитить и запушить
git add .
git commit -m "Desktop app with GitHub Actions build"
git push origin main

# Открыть страницу Actions
start https://github.com/daloestt55/connecta/actions

# Ждите ~5-10 минут
# Скачайте artifacts после успешной сборки
```

---

## ❓ Troubleshooting

### Сборка падает с ошибкой
1. GitHub → Actions → Кликните на failed запуск
2. Читайте логи шага с ❌
3. Обычные проблемы:
   - **Missing dependencies**: `npm ci` провалился → проверьте package.json
   - **Build failed**: Ошибка в коде → проверьте локально `npm run build:win`
   - **Upload artifacts failed**: Нет файлов в release/ → проверьте electron-builder.json

### Нет artifacts после сборки
- Убедитесь что сборка завершилась успешно (✅)
- Прокрутите страницу запуска до конца
- Artifacts внизу, под всеми логами

### Хочу собрать для macOS / Linux
1. Откройте `.github/workflows/electron-build.yml`
2. Раскомментируйте секции `build-macos` или `build-linux`
3. Добавьте скрипты в package.json:
   ```json
   "build:mac": "cross-env ELECTRON=true vite build && electron-builder --mac",
   "build:linux": "cross-env ELECTRON=true vite build && electron-builder --linux"
   ```
4. Закоммитьте и запушьте

---

## 📚 Дополнительно

### Просмотр всех сборок
```powershell
# В браузере
start https://github.com/daloestt55/connecta/actions/workflows/electron-build.yml
```

### Автоматические релизы
Для каждого тега автоматически создается GitHub Release:
```powershell
git tag v1.0.1
git push origin v1.0.1
# → Релиз появится в https://github.com/daloestt55/connecta/releases
```

### Badge в README
Добавьте в README.md:
```markdown
![Build](https://github.com/daloestt55/connecta/actions/workflows/electron-build.yml/badge.svg)
```

---

## 🎯 Итого

**Сейчас нужно сделать:**
1. ✅ Workflow уже создан (electron-build.yml)
2. ⏳ Выполните: `git add . && git commit -m "Desktop build" && git push`
3. ⏳ Откройте GitHub Actions и наблюдайте за сборкой
4. ⏳ Скачайте готовый установщик из Artifacts

**Время сборки:** 5-10 минут  
**Результат:** Connecta-Setup-1.0.0.exe готов к установке!

---

## 💡 Совет

Добавьте в `.gitignore`:
```
release/
dist/
dist-electron/
```

Чтобы не коммитить локальные сборки.
